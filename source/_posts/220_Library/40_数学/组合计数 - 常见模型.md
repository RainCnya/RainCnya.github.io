---
title: 组合计数 - 常见模型
tags:
  - 组合/
date: 2026-03-17
---

# 组合计数 - 常见模型

## Ex. 1 [P5520 青原樱](https://www.luogu.com.cn/problem/P5520)

### 1. 题面梗概

有多少种方案在 $n$ 个位置种 $m$ 株不同的树，使得树两两不相邻。

> $n \leq 2 \times 10^6, m \leq 10^6$

### 2. 逻辑推导

对于本题来说，直接对着构造取计数看起来会很困难。因此可以考虑使用一些其他的方法来进行解题。

采用插空的方法，由于需要选取 $m$ 个位置种树，因此共有 $n - m$ 个位置不种树。这 $n - m$ 个位置之间有 $n - m + 1$ 个空位，加上两端边界的两个空位，共有 $n - m + 1$ 个空位，只需要将树种在这些空位中即可。

显然，两棵树不可能种在同一个空位中。故最终的答案为 $A_{n-m+1}^{m}$。

## Ex. 2 [P4071 排列计数](https://www.luogu.com.cn/problem/P4071)

### 1. 题面梗概

求有多少种从 $1$ 到 $n$ 的排列 $a$，满足序列中恰好有 $m$ 个位置 $i$，使得 $a_{i} = i$。

>多测 $T \leq 5 \times 10^5, n,m \leq 10^6$

### 2. 逻辑推导

不妨先来考虑 $m = 0$ 的情况，也就是所有数字全部错位的情况。

发现很难直接用组合数表示出本题的答案，因此考虑使用其他方式进行求解。不妨设有 $n$ 个数的时候答案为 $d_{n}$，则尝试寻找 $d$ 数组的递推关系。

考虑第 $a_{n}$ 的取值，显然，$a_{n}$ 有 $n - 1$ 种取值，且每种取值等价。不妨设 $a_{n} = 1$，那么来考虑 $a_{1}$ 的取值，则有以下两种情况：

1. $a_{1} = n$。在这种情况下，$a_{1}$ 和 $a_{n}$ 是互相对应的关系，可以直接去掉这两个数字，剩下的就是 $2 \to n-1$ 的数字，答案就是 $d_{n-2}$。

2. $a_{1} \neq n$。在这种情况下，$a_{1}$ 和 $a_{n}$ 就不是互相对应的关系了，也就是剩下来 $1 \to n-1$ 的数字错排序，答案就是 $d_{n-1}$。

所以可以得出 $n$ 个数完全错拍的递推式为：$d_{n} = (n-1)(d_{n-1} + d_{n-2})$。

接着我们分析 $m \neq 0$ 的情况，也就是有 $m$ 个数时完全对应的，那么 $n$ 选 $m$ 就是经典的 $C_{n}^{m}$，然后剩下的 $n - m$ 个数完全错排，最终答案就是：

$$
C_{n}^{m} \times d_{n-m}
$$

由于询问的次数很多，所以需要预处理出 $d$ 数组的值，而对于组合数，这里可以采用逆元加速。

### 3. 代码实现

{% fold info @AcCode %}
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int maxn = 1e6 + 5;
const int mod = 1e9 + 7;

ll qpow( ll a, ll k )
{
    ll res = 1;
    for( ; k; k >>= 1, a = a * a % mod )
        if( k & 1 ) res = res * a % mod;
    return res;
}

ll d[maxn];
ll fac[maxn], inv[maxn];

void init( int N )
{
    d[0] = d[1] = 0, d[2] = 1, d[3] = 2;
    for( int i = 4; i <= N; ++ i ) d[i] = ( i - 1 ) * ( d[i-1] + d[i-2] ) % mod;
    fac[0] = 1;
    for( int i = 1; i <= N; ++ i ) fac[i] = fac[i-1] * i % mod;
    inv[N] = qpow( fac[N], mod - 2 );
    for( int i = N - 1; i >= 0; -- i ) inv[i] = inv[i+1] * ( i + 1 ) % mod;
}

ll get_C( ll n, ll m ) 
{ 
    if( n < m ) return 0;
    return fac[n] * inv[m] % mod * inv[n - m] % mod; 
}

void solve( )
{
    int n, m;
    cin >> n >> m;
    if( n == m ) { cout << 1 << '\n'; return ; }
    cout << ( d[n - m] * get_C( n, m ) % mod ) << '\n';
}

int main( )
{
    ios::sync_with_stdio( 0 ); cin.tie( 0 );
    init( maxn - 5 );
    int _t = 1; cin >> _t;
    while( _t -- ) solve( );
    return 0;
}
```
{% endfold %}

## Ex.3 